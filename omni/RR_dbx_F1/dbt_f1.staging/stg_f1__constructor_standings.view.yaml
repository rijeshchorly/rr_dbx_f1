# Reference this view as dbt_f1_staging__stg_f1__constructor_standings
#This description was pulled from dbt.
description: Cumulative constructor championship standings after each race.

catalog: dbt_f1
schema: staging
table_name: stg_f1__constructor_standings

dimensions:
  points: {}
  standings_position: {}
  position_text: {}
  wins: {}

  constructor_standings_id:
    format: ID

  race_id:
    format: ID

  constructor_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_f1__constructor_standings
  target_schema: prod
  description: Cumulative constructor championship standings after each race.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (

        select * from {{ source('f1', 'constructor_standings') }}

    ),

    renamed as (

        select
            constructorStandingsId as constructor_standings_id,
            raceId as race_id,
            constructorId as constructor_id,
            points,
            cast({{ ergast_null('position') }} as int) as standings_position,
            positionText as position_text,
            wins

        from source

    )

    select * from renamed
  referenced_by: [ fct_constructor_standings ]
