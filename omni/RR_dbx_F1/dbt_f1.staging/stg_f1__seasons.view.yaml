# Reference this view as dbt_f1_staging__stg_f1__seasons
#This description was pulled from dbt.
description: One record per championship season.

catalog: dbt_f1
schema: staging
table_name: stg_f1__seasons

dimensions:
  season_year: {}
  wikipedia_url: {}

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_f1__seasons
  target_schema: prod
  description: One record per championship season.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (

        select * from {{ source('f1', 'seasons') }}

    ),

    renamed as (

        select
            year as season_year,
            url as wikipedia_url

        from source

    )

    select * from renamed
