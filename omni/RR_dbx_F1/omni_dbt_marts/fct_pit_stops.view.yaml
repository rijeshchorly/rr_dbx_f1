# Reference this view as omni_dbt_marts__fct_pit_stops
schema_label: ""
#This description was pulled from dbt.
description: One record per pit stop (2011 onwards).

schema: omni_dbt_marts
table_name: fct_pit_stops

dimensions:
  pit_stop_key: {}
  stop_number: {}
  lap_number: {}
  pit_stop_local_time: {}
  duration_display: {}
  pit_stop_milliseconds: {}

  race_id:
    format: ID

  driver_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: fct_pit_stops
  target_schema: prod
  description: One record per pit stop (2011 onwards).
  config:
    schema: marts
    materialized: table
  code: |-
    select
        {{ dbt_utils.generate_surrogate_key(['race_id', 'driver_id', 'stop_number']) }} as pit_stop_key,
        race_id,
        driver_id,
        stop_number,
        lap_number,
        pit_stop_local_time,
        duration_display,
        pit_stop_milliseconds

    from {{ ref('stg_f1__pit_stops') }}
