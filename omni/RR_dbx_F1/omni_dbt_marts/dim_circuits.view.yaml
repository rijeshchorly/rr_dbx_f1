# Reference this view as omni_dbt_marts__dim_circuits
schema_label: ""
#This description was pulled from dbt.
description: One record per circuit.

schema: omni_dbt_marts
table_name: dim_circuits

dimensions:
  circuit_ref: {}
  circuit_name: {}
  location: {}
  country: {}
  latitude: {}
  longitude: {}
  altitude_metres: {}
  wikipedia_url: {}

  circuit_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: dim_circuits
  target_schema: prod
  description: One record per circuit.
  config:
    schema: marts
    materialized: table
  code: |-
    select
        circuit_id,
        circuit_ref,
        circuit_name,
        location,
        country,
        latitude,
        longitude,
        altitude_metres,
        wikipedia_url

    from {{ ref('stg_f1__circuits') }}
