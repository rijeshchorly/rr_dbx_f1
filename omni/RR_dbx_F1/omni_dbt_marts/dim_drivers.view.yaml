# Reference this view as omni_dbt_marts__dim_drivers
schema_label: ""
#This description was pulled from dbt.
description: One record per driver.

schema: omni_dbt_marts
table_name: dim_drivers

dimensions:
  driver_ref: {}
  driver_number: {}
  driver_code: {}
  forename: {}
  surname: {}
  full_name: {}
  date_of_birth: {}
  nationality: {}
  wikipedia_url: {}

  driver_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: dim_drivers
  target_schema: prod
  description: One record per driver.
  config:
    schema: marts
    materialized: table
  code: |-
    select
        driver_id,
        driver_ref,
        driver_number,
        driver_code,
        forename,
        surname,
        full_name,
        date_of_birth,
        nationality,
        wikipedia_url

    from {{ ref('stg_f1__drivers') }}
