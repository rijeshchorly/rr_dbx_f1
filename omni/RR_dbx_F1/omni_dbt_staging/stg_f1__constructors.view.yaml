# Reference this view as omni_dbt_staging__stg_f1__constructors
schema_label: ""
#This description was pulled from dbt.
description: One record per constructor (team).

schema: omni_dbt_staging
table_name: stg_f1__constructors

dimensions:
  constructor_ref: {}
  constructor_name: {}
  nationality: {}
  wikipedia_url: {}

  constructor_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_f1__constructors
  target_schema: prod
  description: One record per constructor (team).
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (

        select * from {{ source('f1', 'constructors') }}

    ),

    renamed as (

        select
            constructorId as constructor_id,
            constructorRef as constructor_ref,
            name as constructor_name,
            nationality,
            url as wikipedia_url

        from source

    )

    select * from renamed
  referenced_by: [ dim_constructors ]
