# Reference this view as omni_dbt_staging__stg_f1__constructor_results
schema_label: ""
#This description was pulled from dbt.
description: Constructor points per race; status_note D marks a disqualification.

schema: omni_dbt_staging
table_name: stg_f1__constructor_results

dimensions:
  points: {}
  status_note: {}

  constructor_results_id:
    format: ID

  race_id:
    format: ID

  constructor_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_f1__constructor_results
  target_schema: prod
  description: Constructor points per race; status_note D marks a disqualification.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (

        select * from {{ source('f1', 'constructor_results') }}

    ),

    renamed as (

        select
            constructorResultsId as constructor_results_id,
            raceId as race_id,
            constructorId as constructor_id,
            points,
            {{ ergast_null('status') }} as status_note

        from source

    )

    select * from renamed
