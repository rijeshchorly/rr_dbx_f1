# Reference this view as omni_dbt_staging__stg_f1__circuits
schema_label: ""
#This description was pulled from dbt.
description: One record per circuit.

schema: omni_dbt_staging
table_name: stg_f1__circuits

dimensions:
  circuit_ref: {}
  circuit_name: {}
  location: {}
  country: {}
  latitude: {}
  longitude: {}
  altitude_metres: {}
  wikipedia_url: {}

  circuit_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_f1__circuits
  target_schema: prod
  description: One record per circuit.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (

        select * from {{ source('f1', 'circuits') }}

    ),

    renamed as (

        select
            circuitId as circuit_id,
            circuitRef as circuit_ref,
            name as circuit_name,
            location,
            country,
            lat as latitude,
            lng as longitude,
            alt as altitude_metres,
            url as wikipedia_url

        from source

    )

    select * from renamed
  referenced_by: [ dim_circuits, dim_races ]
