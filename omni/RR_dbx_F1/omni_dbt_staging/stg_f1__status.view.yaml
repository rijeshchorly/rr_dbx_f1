# Reference this view as omni_dbt_staging__stg_f1__status
schema_label: ""
#This description was pulled from dbt.
description: Lookup of finishing statuses.

schema: omni_dbt_staging
table_name: stg_f1__status

dimensions:
  status: {}

  status_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_f1__status
  target_schema: prod
  description: Lookup of finishing statuses.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (

        select * from {{ source('f1', 'status') }}

    ),

    renamed as (

        select
            statusId as status_id,
            status

        from source

    )

    select * from renamed
  referenced_by: [ fct_race_results ]
